name: Coleta GA4 → Google Sheets → data.json

on:
  schedule:
    # 08:00 BRT (UTC-3) = 11:00 UTC
    # No horário de verão (out–fev) BRT = UTC-2, então roda às 09h BRT
    # Aceitável — dados do GA4 do dia anterior estão completos a qualquer hora
    - cron: "0 11 * * *"
  workflow_dispatch:
    inputs:
      force_historical:
        description: "Forçar modo histórico (recoleta tudo desde jan/2025)"
        required: false
        default: "false"
        type: choice
        options:
          - "false"
          - "true"

jobs:
  fetch-and-export:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    permissions:
      contents: write  # necessário para commitar o data.json

    steps:
      - name: Checkout do repositório
        uses: actions/checkout@v4.2.2

      - name: Configurar Python
        uses: actions/setup-python@v5.3.0
        with:
          python-version: "3.11"

      - name: Instalar dependências
        run: |
          pip install \
            google-auth \
            google-api-python-client

      - name: Passo 1 — Coletar GA4 → Google Sheets
        env:
          GA4_CREDENTIALS_JSON: ${{ secrets.GA4_CREDENTIALS_JSON }}
          SPREADSHEET_ID:       ${{ secrets.SPREADSHEET_ID }}
          FORCE_HISTORICAL:     ${{ github.event.inputs.force_historical || 'false' }}
        run: python scripts/fetch_ga4.py

      - name: Passo 2 — Exportar Sheets → data.json
        env:
          GA4_CREDENTIALS_JSON: ${{ secrets.GA4_CREDENTIALS_JSON }}
          SPREADSHEET_ID:       ${{ secrets.SPREADSHEET_ID }}
        run: python scripts/export_data.py

      - name: Passo 3 — Commitar data.json no repositório
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          # Pull antes do push para evitar conflito caso o Claude Cowork
          # tenha feito commit (ex: atualização de insights) entre o início
          # deste workflow e este passo
          git pull --rebase origin main
          git add data.json
          git diff --cached --quiet || git commit -m "chore: atualiza data.json [$(date -u '+%Y-%m-%d %H:%M')] UTC"
          git push
